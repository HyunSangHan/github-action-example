name: "11 Permissions"

on:
  workflow_dispatch:

jobs:
  example-job:
    permissions:
      contents: write # can commit to the repository
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Attempt Write Operation
        run: |
          touch new-file.txt
          git config --global user.name 'Hyunsang Han'
          git config --global user.email 'gustkd3@gmail.com'
          git add new-file.txt
          git commit -m "Attempt to write"
          git push origin ${{ github.ref }}
